name: Update Submodules

on:
  repository_dispatch:
    types: [ update-submodules ]

jobs:
  update_modules:
    name: Update Submodules
    runs-on: ubuntu-latest
    steps:
      - name: Validate inputs
        run: |
          [[ "${{ github.event.repo_name }}" ]] || { echo "Input \"repo_name\" cannot be empty" ; exit 1; }
          [[ "${{ github.event.tag }}" ]] || { echo "Input \"tag\" cannot be empty" ; exit 1; }
    
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Update submodule
        working-directory: docs/submodules/${{ inputs.repo-name }}
        run: git checkout ${{ inputs.tag }}

      - name: Update parent repository
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add .
          git commit --allow-empty -m "Updates ${{ inputs.repo-name }} to ${{ inputs.tag }}"
          git push
